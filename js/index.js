/*function hello(){
	alert (hello devi)
}*/
const isArmstrong = () =>{
	le inputtagedocument.getElementByElementById('input').value.split('').map(digit=> parsInt(digit))
	let digit = userinput.split('')
	let digits = userinput.split('')
	let sum = 0
	for (let digit of digitscube){
		sum += digit 
	}
	for (let digit of digits){
		digitInt
	}
	if (sum.string === userinput) 
		if 
}
